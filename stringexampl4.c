//String in c language
#include<stdio.h>
void main()
{
    char name[50];
    printf("enter a name :");
    gets(name);// its take string from keybord
    puts(name);// its display on consol screen
   // printf("name = %s\n",name);
  
}